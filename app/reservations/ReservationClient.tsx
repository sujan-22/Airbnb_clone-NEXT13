import { SafeReservations, SafeUser } from "../types";

interface Props {}

const ReservationClient: React.FC<Props> = ({ props }) => {
    return <div>Reservations on my property</div>;
};

export default ReservationClient;
